/** Anti-répétition — phase 2 (déduplication sémantique des bulles). */
export function isRepeatedAssistantReply(_sessionId: string, _content: string): Promise<boolean> {
  return Promise.resolve(false);
}
